document.addEventListener('DOMContentLoaded',()=>{document.querySelector('.summary-cards')?.remove();document.querySelector('.page-actions a[href="推送异常预警.html"]')?.remove();const $=s=>document.querySelector(s),rows=[...document.querySelectorAll('#planTable tbody tr')],modal=$('#planModal');const render=()=>{const key=$('#keyword').value.trim(),project=$('#project').value,status=$('#status').value;rows.forEach(row=>{const show=(!key||row.textContent.includes(key))&&(project==='all'||row.dataset.project===project)&&(status==='all'||row.dataset.status===status);row.hidden=!show});$('#empty').hidden=rows.some(row=>!row.hidden)};const close=()=>{modal.classList.remove('show');modal.setAttribute('aria-hidden','true')};$('#query').addEventListener('click',render);$('#reset').addEventListener('click',()=>{$('#keyword').value='';$('#project').value='all';$('#status').value='all';$('#start').value='';$('#end').value='';render()});document.querySelector('#planTable tbody').addEventListener('click',e=>{const row=e.target.closest('tr');if(!row)return;if(e.target.closest('.plan-view')){const title=row.querySelector('.plan-name b').textContent;$('#modalTitle').textContent=title+' · 计划详情';$('#detailProject').textContent=row.dataset.project;$('#detailUsers').textContent=row.dataset.users+' 人';$('#detailTag').textContent=title.includes('心理')?'心理风险关注':title.includes('靶向')?'靶向治疗患者':title.includes('老年')?'老年患者':'高依从性患者';$('#detailNode').textContent=row.querySelector('.plan-name small').textContent.replace('执行','推送');modal.classList.add('show');modal.setAttribute('aria-hidden','false')}if(e.target.closest('.plan-status')){const online=row.dataset.status==='online';row.dataset.status=online?'offline':'online';const pill=row.querySelector('.pill'),button=row.querySelector('.plan-status');pill.textContent=online?'已下架':'生效中';pill.className='pill '+(online?'gray':'green');button.textContent=online?'上架':'下架';render()}});$('#closeModal').addEventListener('click',close);$('#modalConfirm').addEventListener('click',close);modal.addEventListener('click',e=>{if(e.target===modal)close()});render()});
