document.addEventListener('DOMContentLoaded',()=>{const params=new URLSearchParams(location.search),plan=params.get('plan');if(plan)document.querySelector('#title').textContent=plan+' · 匹配用户';const input=document.querySelector('#keyword'),rows=[...document.querySelectorAll('#userTable tbody tr')],empty=document.querySelector('#empty');const render=()=>{let count=0;rows.forEach(row=>{const show=row.textContent.includes(input.value.trim());row.hidden=!show;if(show)count++});empty.hidden=!!count;document.querySelector('#matchedCount').firstChild.nodeValue=count+' '};document.querySelector('#query').addEventListener('click',render);document.querySelector('#reset').addEventListener('click',()=>{input.value='';render()});render()});
