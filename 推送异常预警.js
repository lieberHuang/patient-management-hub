document.addEventListener('DOMContentLoaded',()=>{const $=s=>document.querySelector(s),rows=[...document.querySelectorAll('#warningTable tbody tr')],modal=$('#pauseModal');const render=()=>{const status=$('#status').value,key=$('#keyword').value.trim();let count=0;rows.forEach(row=>{const show=(status==='all'||row.dataset.status===status)&&(!key||row.textContent.includes(key));row.hidden=!show;if(show&&row.dataset.status==='pending')count++});$('#empty').hidden=rows.some(row=>!row.hidden);$('#pendingCount').firstChild.nodeValue=count+' '};const close=()=>{modal.classList.remove('show');modal.setAttribute('aria-hidden','true')};$('#query').addEventListener('click',render);$('#reset').addEventListener('click',()=>{$('#status').value='all';$('#keyword').value='';$('#start').value='';$('#end').value='';render()});document.querySelector('#warningTable tbody').addEventListener('click',e=>{const row=e.target.closest('tr');if(!row)return;if(e.target.closest('.resolve')){row.dataset.status='done';row.querySelector('.pill').textContent='已处理';row.querySelector('.pill').className='pill gray';row.querySelector('td:last-child').innerHTML='<span style="color:#8795a6">—</span>';render()}if(e.target.closest('.pause')){modal.dataset.row=[...rows].indexOf(row);modal.classList.add('show');modal.setAttribute('aria-hidden','false')}});['#closePause','#cancelPause'].forEach(sel=>$(sel).addEventListener('click',close));$('#confirmPause').addEventListener('click',()=>{const row=rows[Number(modal.dataset.row)];if(row){row.dataset.status='done';row.querySelector('.pill').textContent='已处理';row.querySelector('.pill').className='pill gray';row.querySelector('td:last-child').innerHTML='<span style="color:#8795a6">已暂停相关推送</span>';render()}close()});modal.addEventListener('click',e=>{if(e.target===modal)close()});render()});
